{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/spatiallattice/master/nextflow_schema.json",
    "title": "nf-core/spatiallattice pipeline parameters",
    "description": "pipeline for processing and integration of MACSima multiplexed imaging with H&E stained tissue slides",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["input", "outdir"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to comma-separated file containing information about the samples in the experiment.",
                    "help_text": "You will need to create a design file with information about the samples in your experiment before running the pipeline. Use this parameter to specify its location. It has to be a comma-separated file with 2 columns, and a header row. See [usage docs](https://nf-co.re/spatiallattice/usage#samplesheet-input).",
                    "fa_icon": "fas fa-file-csv"
                },
                "project": {
                    "type": "boolean",
                    "description": "Set to true to run processing steps on a full project or study with multiple experiments and ROIs. Use when providing the samplesheet for a ful project or study.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": false,
                    "help_text": "Enable this option if you are providing a samplesheet for a full project or study with multiple experiments and ROIs. See the example samplesheet_project."
                },
                "illumination_correction": {
                    "type": "boolean",
                    "description": "Give this flag to apply illumination correction to all tiles, the illumination profiles are calculated with basicpy.",
                    "fa_icon": "fas fa-lightbulb",
                    "default": false,
                    "help_text": "Enable this option to run the optional illumination correction step on the input images with basicpy."
                },
                "hi_exposure_only": {
                    "type": "boolean",
                    "description": "Give this flag to extract only the set of images with the highest exposure time.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "default": false,
                    "help_text": "Enable this option to extract only the set of images with the highest exposure time. All downstream processing steps will use only the high exposure images."
                },
                "remove_reference_marker": {
                    "type": "boolean",
                    "description": "mark the removal of the reference marker ,e.g. DAPI, for all cycles except the first one.",
                    "fa_icon": "fas fa-times-circle",
                    "default": false,
                    "help_text": "mark the removal of the reference marker ,e.g. DAPI, for all cycles except the first one."
                },
                "qc_metrics": {
                    "type": "boolean",
                    "description": "measure features of contrast, intensity and sharpness of each tile in the cycle and appends them to a table.",
                    "fa_icon": "fas fa-chart-bar",
                    "default": false,
                    "help_text": "measure features of contrast, intensity and sharpness of each tile in the cycle and appends them to a table."
                },
                "write_table": {
                    "type": "boolean",
                    "description": "Writes a table with the acquisition parameters, metadata and,if enabled, qc metrics of each tile.",
                    "fa_icon": "fas fa-table",
                    "default": false,
                    "help_text": "Writes a table with the acquisition parameters, metadata and,if enabled, qc metrics of each tile."
                },
                "background_subtraction": {
                    "type": "boolean",
                    "description": "Performs pixel-by-pixel background subtraction between marker and background channels scaled by their respective exposure times.",
                    "fa_icon": "fas fa-eraser",
                    "default": false,
                    "help_text": "Performs pixel-by-pixel background subtraction between marker and background channels scaled by their respective exposure times."
                },
                "stainsegmy": {
                    "type": "boolean",
                    "description": "Run nuclei segmentation on provided H&E images.",
                    "fa_icon": "fas fa-cut",
                    "default": false,
                    "help_text": "Run nuclei segmentation on provided H&E images."
                },
                "cellpose": {
                    "type": "boolean",
                    "description": "Run the cell segmentation step (Cellpose) on the stitched and registered images.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": false,
                    "help_text": "Enable this option to run the optional cell segmentation step. This will run Cellpose on the ASHLAR output. The markersheet required by Cellpose is generated by the macsima2mc staging step."
                },
                "cellpose_pretrained_model": {
                    "type": "string",
                    "description": "Cellpose model to use for cell segmentation.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": "cyto3",
                    "help_text": "Specify the Cellpose model to use for cell segmentation."
                },
                "cellpose_custom_model": {
                    "type": "string",
                    "description": "Path to custom Cellpose model to use for cell segmentation.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": null,
                    "help_text": "Specify the path to a custom Cellpose model to use for cell segmentation. If this is set, the pretrained model will be ignored."
                },
                "cellpose_save_flows": {
                    "type": "boolean",
                    "description": "whether or not to save RGB images of flows when masks are saved.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": false,
                    "help_text": "whether or not to save RGB images of flows when masks are saved."
                },
                "cellpose_diameter": {
                    "type": "number",
                    "description": "Use to resize cells to the training diameter (30 pixels)",
                    "fa_icon": "fas fa-project-diagram",
                    "default": 30,
                    "help_text": "use to resize cells to the training diameter (30 pixels)"
                },
                "cellpose_chan": {
                    "type": "number",
                    "description": "Cellpose channel to use for cell segmentation.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": 0,
                    "help_text": "Specify the Cellpose channel to use for cell segmentation. This is the channel index of the image to use for segmentation. The default is 0, which is the first channel."
                },
                "cellpose_chan2": {
                    "type": "number",
                    "description": "Cellpose second channel to use for cell segmentation.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": null,
                    "help_text": "Specify the Cellpose second channel to use for cell segmentation. This is the channel index of the second image to use for segmentation. The default is null, which means that no second channel will be used."
                },
                "cellpose_flow_threshold": {
                    "type": "number",
                    "description": "flow error threshold, 0 turns off this optional QC step. Default: 0.4",
                    "fa_icon": "fas fa-project-diagram",
                    "default": 0.4,
                    "help_text": "flow error threshold, 0 turns off this optional QC step. Default: 0.4"
                },
                "cellpose_model": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to a custom Cellpose model file to use for cell segmentation.",
                    "fa_icon": "fas fa-project-diagram",
                    "default": null,
                    "help_text": "Specify the path to a custom Cellpose model file to use for cell segmentation. This is passed directly to the Cellpose module as the model input. If not set, the model is selected via the cellpose_pretrained_model or cellpose_custom_model parameters."
                },
                "stainwarpy": {
                    "type": "boolean",
                    "description": "Run the StainWarPy registration step to register the H&E image (moving) to the immunofluorescence image (fixed).",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": false,
                    "help_text": "Enable this option to run the optional StainWarPy registration step. This registers the H&E image to the immunofluorescence image using stainwarpy."
                },
                "fixed_img": {
                    "type": "string",
                    "description": "Channel of the fixed image to be used for registration. 0-based index or string h, e or d to convert H&E into Hematoxylin, Eosin or DAB.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "0-based index of the channel of the fixed image to be used for registration. If a string is given (h, e or d) it converts the H&E image into Hematoxylin, Eosin or DAB."
                },
                "final_sz": {
                    "type": "string",
                    "description": "Image resolution in micrometers to be used for applying the registration scheme.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Image resolution in micrometers (microns per pixel) to be used for applying the registration scheme. Should be at least 2-3 times smaller than the registration feature size."
                },
                "rami2d": {
                    "type": "boolean",
                    "description": "Run the RAMI2D registration step to register the H&E image (moving) to the immunofluorescence image (fixed).",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": false,
                    "help_text": "Enable this option to run the optional RAMI2D registration step. This registers the H&E image to the immunofluorescence image using rami2d-register."
                },
                "rami2d_mpp_fix": {
                    "type": "number",
                    "description": "Pixel size in micrometers of the fixed image.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Pixel size in micrometers (microns per pixel) of the fixed image used for registration."
                },
                "rami2d_ifix": {
                    "type": ["string", "integer"],
                    "description": "Channel of the fixed image to be used for registration. 0-based index or string h, e or d to convert H&E into Hematoxylin, Eosin or DAB.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "0-based index of the channel of the fixed image to be used for registration. If a string is given (h, e or d) it converts the H&E image into Hematoxylin, Eosin or DAB."
                },
                "rami2d_mpp_mov": {
                    "type": "number",
                    "description": "Pixel size in micrometers of the moving image.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Pixel size in micrometers (microns per pixel) of the moving image used for registration."
                },
                "rami2d_imov": {
                    "type": ["string", "integer"],
                    "description": "Channel of the moving image to be used for registration. 0-based index or string h, e or d to convert H&E into Hematoxylin, Eosin or DAB.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "0-based index of the channel of the moving image to be used for registration. If a string is given (h, e or d) it converts the H&E image into Hematoxylin, Eosin or DAB."
                },
                "rami2d_mpp_reg": {
                    "type": "number",
                    "description": "Image resolution in micrometers to be used for applying the registration scheme.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Image resolution in micrometers (microns per pixel) to be used for applying the registration scheme. Should be at least 2-3 times smaller than the registration feature size."
                },
                "rami2d_transform": {
                    "type": "boolean",
                    "description": "Run the RAMI2D transform step to apply the transformation maps to images associated with the moving image.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": false,
                    "help_text": "Enable this option to run the optional RAMI2D transform step. This applies the transformation maps produced by rami2d-register to images associated with the moving image using rami2d-transform."
                },
                "rami2d_transform_input": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Path to the image to be transformed.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Path to the image to be transformed by rami2d-transform."
                },
                "rami2d_transform_mpp": {
                    "type": "number",
                    "description": "Pixel size in micrometers of the image to be transformed.",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Pixel size in micrometers (microns per pixel) of the image to be transformed."
                },
                "rami2d_transform_tdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Path to the folder with the transformation map files (.txt).",
                    "fa_icon": "fas fa-arrows-alt",
                    "default": null,
                    "help_text": "Path to the folder containing the transformation map files (.txt) produced by rami2d-register."
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                }
            }
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "format": "file-path",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "multiqc_logo": {
                    "type": "string",
                    "description": "Custom logo file to supply to MultiQC. File name must also be set in the MultiQC config file",
                    "fa_icon": "fas fa-image",
                    "hidden": true
                },
                "multiqc_methods_description": {
                    "type": "string",
                    "description": "Custom MultiQC yaml file containing HTML including a methods description.",
                    "fa_icon": "fas fa-cog"
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "pipelines_testdata_base_path": {
                    "type": "string",
                    "fa_icon": "far fa-check-circle",
                    "description": "Base URL or local path to location of pipeline test dataset files",
                    "default": "https://raw.githubusercontent.com/nf-core/test-datasets/",
                    "hidden": true
                },
                "trace_report_suffix": {
                    "type": "string",
                    "fa_icon": "far calendar",
                    "description": "Suffix to add to the trace report filename. Default is the date and time in the format yyyy-MM-dd_HH-mm-ss.",
                    "hidden": true
                },
                "help": {
                    "type": ["boolean", "string"],
                    "description": "Display the help message."
                },
                "help_full": {
                    "type": "boolean",
                    "description": "Display the full detailed help message."
                },
                "show_hidden": {
                    "type": "boolean",
                    "description": "Display hidden parameters in the help message (only works when --help or --help_full are provided)."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        }
    ]
}
