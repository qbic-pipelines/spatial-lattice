{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/spatiallattice/master/assets/schema_input_project.json",
    "title": "nf-core/spatiallattice pipeline - params.input schema project",
    "description": "Schema for the file provided with params.input and params.project",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "project": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "project name must be provided and cannot contain spaces",
                "meta": ["project"]
            },
            "MACSima_project_folder": {
                "type": "string",
                "format": "directory-path",
                "exists": true,
                "pattern": "^\\S+$",
                "errorMessage": "Path to the project folder of the  MACSima experiment must be provided and cannot contain spaces"
            },
            "HnE_folder": {
                "type": "string",
                "format": "directory-path",
                "exists": true,
                "pattern": "^\\S+$",
                "errorMessage": "Path to the H&E folder must be provided and cannot contain spaces"
            }
        },
        "required": ["project", "MACSima_project_folder"]
    }
}
